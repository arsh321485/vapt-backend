# Python
__pycache__/
*.pyc
*.pyo
*.pyd
*.sqlite3

# Virtualenv
venv/
env/

# Django
*.log
*.pot
*.pyc
db.sqlite3
media/
staticfiles/

# VS Code
.vscode/

# Environment variables
.env

# Django file-based cache
cache/
django_cache/
*.djcache
prompt.txt

# Dead-code backups — kept on disk (and in git HISTORY, already committed
# once) for recovery, but excluded from tracking going forward so SAST
# scanners (Bandit/Semgrep) stop flagging unused, never-executed code.
_backups/
automation_scripts_old_backup/
